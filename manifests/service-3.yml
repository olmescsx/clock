apiVersion: v1
kind: Service
metadata:
    name: "aksolmes-6250"
    labels:
        app: "aksolmes-6250"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "aksolmes-6250"